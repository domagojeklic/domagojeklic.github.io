---
layout: post
title: LLDB automatically import UIKit inside Xcode
---

```objective_c
- (NSURL *)verificationServerURL {
    NSURLComponents *components = [[NSURLComponents alloc] init];
    components.scheme = HttpScheme;
    components.host = VerificationHost;
    components.port = @(VerificationPort);

    return [components URL];
}
```
